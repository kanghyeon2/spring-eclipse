<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.insa.mapper.EmployeesMapper">
<!-- 전체조회 getList -->
<select id="getList" resultType="EmployeesDTO">
            SELECT 
               employee_id, 
               first_name, 
               last_name,
               email,
               phone_number,
               hire_date,
               job_id,
               salary,
               commission_pct,
               manager_id,              
               department_id
            FROM   
               employees
</select>

<!-- 등록 insert -->
<insert id="insert" parameterType="employeesDTO">
	insert into tbl_board(employee_id, first_name, last_name, email, hire_date, job_id)
	 			  values (#{employeeId}, #{firstName}, #{lastName}, #{email}, #{hireDate}, #{jobId})
</insert>
</mapper>